import React from "react";
import styled from "styled-components";

const StyledTableHeader = styled.div`
  display: flex;
  align-items: center;
  flex-shrink: 0;
`;

const TableHeader = () => {
  return <StyledTableHeader>{/*  */}</StyledTableHeader>;
};

export default TableHeader;
